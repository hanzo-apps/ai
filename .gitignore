# Environment variables - NEVER commit credentials
.env
.env.local
.env.*.local
.env.production
.env.staging
*.secrets

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.next
# NEXT_DIST_DIR lets parallel builds pick their own dir (next.config.ts); every
# one of them is a build artifact.
.next-*
out
*.tsbuildinfo
next-env.d.ts
playwright-report
test-results
tests/screenshots

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

AGENTS.md
CLAUDE.md
GEMINI.md
QWEN.md
.next-verify/
.next-dev/
.claude/

# Agent scratchpad / build caches must never be committed.
tmp/

# Stray local IAM SQLite DB — never commit a database into the marketing site.
iam.db
*.db

# scratch next build output
.next-uidefect/

# stray probe scripts
.probe-tmp.mjs
_*.mjs
