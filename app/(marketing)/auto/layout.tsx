import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Workflow automation in one binary',
  description: 'Draw a workflow, connect the apps it touches, and fire it from a webhook or a schedule. Editor, connector catalog, database and durable executor all in one Go process.',
  path: '/auto',
})
