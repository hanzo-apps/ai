import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Hanzo Auto',
  description: 'Connect 300+ apps and services with a visual workflow builder. Trigger automations from webhooks, schedules, or events.',
  path: '/auto',
})
