import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Hanzo Tasks',
  description: 'Build invincible applications. Workflows that survive crashes, retries that never give up, and sagas that always compensate. Your code runs to completion…',
  path: '/tasks',
})
