import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Get paid when your open source code powers AI infrastructure',
  description:
    'Hanzo allocates up to 25% of compute costs to OSS authors whose software is running in production. Connect your Git identity, verify contributions, collect automatically.',
  path: '/open-source/dividends',
})
