import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'The Model Context Protocol, hosted',
  description: 'One MCP server that gives any agent 260+ real tools — shell, files, code intelligence, browser, memory, and integrations — with managed hosting, secure…',
  path: '/mcp',
})
