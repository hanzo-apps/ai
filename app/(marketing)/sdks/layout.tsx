import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'SDKs for every language',
  description: 'One cloud, every language. A full Cloud SDK generated from one OpenAPI spec — idiomatic in each ecosystem — plus a hand-crafted AI + Agents flagship. Real…',
  path: '/sdks',
})
