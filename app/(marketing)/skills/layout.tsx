import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Skills',
  description: 'A skill is a markdown file about one thing. Install the plugin and your coding agent reads the right one when the work calls for it, instead of carrying all of them into every session.',
  path: '/skills',
})
