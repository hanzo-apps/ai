import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Hanzo Gateway',
  description: 'One front door for every Hanzo service. Authentication, rate limiting, intelligent routing, and full observability — built into a single high-performance…',
  path: '/gateway',
})
