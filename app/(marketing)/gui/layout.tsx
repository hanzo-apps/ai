import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Hanzo GUI',
  description: 'The cross-platform UI kit and design system that powers every Hanzo surface — web, mobile, desktop. Tokens, primitives, and motion that ship the same code…',
  path: '/gui',
})
