import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'A senior engineer in your shell',
  description: 'One CLI for the whole stack. Plan, edit, and ship code with a model in your terminal — then build, deploy, and tail logs on Hanzo Cloud without leaving…',
  path: '/cli',
})
