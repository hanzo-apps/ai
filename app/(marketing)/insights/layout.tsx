import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Hanzo Insights',
  description: 'Understand user behavior with event analytics, session recording, funnels, and A/B testing. Roll out features safely with feature flags.',
  path: '/insights',
})
