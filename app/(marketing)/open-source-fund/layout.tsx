import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Up to 5% of revenue to open source',
  description: 'Up to 5% of all platform revenue goes directly to the open source developers whose code powers Hanzo infrastructure.',
  path: '/open-source-fund',
})
