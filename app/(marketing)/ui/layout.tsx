import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Hanzo UI',
  description: 'Open-source, accessible, themeable React components built on @hanzo/gui primitives and @hanzo/design tokens. Install them, theme them, ship faster.',
  path: '/ui',
})
