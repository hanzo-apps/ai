import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Cookie Policy',
  description: 'How Hanzo uses cookies and similar storage on hanzo.ai and our other properties.',
  path: '/cookies',
})
