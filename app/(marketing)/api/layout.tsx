import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'One API for models, agents and tools',
  description: 'Every Hanzo capability is reachable from a single base URL with a single key. If you have used the OpenAI API, you already know most of this.',
  path: '/api',
})
