import { pageMeta } from '@/lib/page-meta'
export { default } from '@/lib/meta-layout'

export const metadata = pageMeta({
  title: 'Hanzo World',
  description: 'A live map of every model, agent, and AI workload across your organization. Latency, cost, drift and anomalies, surfaced in real time.',
  path: '/world',
})
